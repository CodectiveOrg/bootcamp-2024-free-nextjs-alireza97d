import { ReactElement } from "react";

import styles from "./load-more.module.css";

const LoadMoreComponent = (): ReactElement => {
  return <button className={styles.btn}>مشاهده بیشتر</button>;
};

export default LoadMoreComponent;
